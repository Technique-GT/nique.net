const Spinner = () => {

  return (
    <div 
      className={`animate-spin rounded-full h-12 w-12 border-2 border-t-transparent border-nique-blue`}
      aria-label="Loading"
    />
  );
};

export default Spinner;
